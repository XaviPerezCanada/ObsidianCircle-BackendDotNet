

namespace MiProyecto.Domain.Reservation
{
    public enum TimeSlot { Morning, Afternoon, Night, FullDay }
    public enum BlockSlot { Morning, Afternoon, Night }
    public enum ReservationStatus { Active, Cancelled }
}
